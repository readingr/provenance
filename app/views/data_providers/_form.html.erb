<%= form_for(@data_provider) do |f| %>
  <% if @data_provider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@data_provider.errors.count, "error") %> prohibited this data_provider from being saved:</h2>

      <ul>
      <% @data_provider.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
